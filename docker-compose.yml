services:
  jarvis:
    build: .
    container_name: jarvis
    working_dir: /app
    ports:
      - "3000:3000"
      - "8741:8741"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
